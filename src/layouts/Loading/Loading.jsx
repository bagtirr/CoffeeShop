import Spinner from "../Spinner/Spinner";

import "./Loading.scss";

const Loading = () => {
    return (
        <section className="loading">
            <Spinner />
        </section>
    );
};

export default Loading;
